apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-spa
  labels:
    app: spa
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: spa
    release: {{ .Release.Name }}
