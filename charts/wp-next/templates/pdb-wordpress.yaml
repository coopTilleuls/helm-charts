apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Values.wordpress.pdb.name }}-pdb
spec:
  maxUnavailable: {{ .Values.wordpress.pdb.maxUnavailable }}
  selector:
    matchLabels:
      app: wordpress
